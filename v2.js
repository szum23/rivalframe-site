(()=>{const p=document.querySelector('.v2-progress');const intel=document.querySelector('.hero-intel');const tabs=[...document.querySelectorAll('[data-stage-btn]')];const method=[...document.querySelectorAll('[data-method-step]')];const bar=document.querySelector('.method-progress i');let stage=0;let timer;const setStage=i=>{stage=i;intel?.setAttribute('data-stage',String(i));tabs.forEach((b,n)=>b.classList.toggle('active',n===i));};const rotate=()=>{clearInterval(timer);timer=setInterval(()=>setStage((stage+1)%4),3200)};tabs.forEach((b,i)=>b.addEventListener('click',()=>{setStage(i);rotate()}));setStage(0);rotate();const onScroll=()=>{const max=Math.max(1,document.documentElement.scrollHeight-innerHeight);if(p)p.style.transform=`scaleX(${Math.min(1,scrollY/max)})`;let active=0;method.forEach((el,i)=>{const r=el.getBoundingClientRect();if(r.top<innerHeight*.58)active=i;});method.forEach((el,i)=>el.classList.toggle('active',i===active));if(bar)bar.style.width=`${25*(active+1)}%`;};addEventListener('scroll',onScroll,{passive:true});addEventListener('resize',onScroll,{passive:true});onScroll();})();